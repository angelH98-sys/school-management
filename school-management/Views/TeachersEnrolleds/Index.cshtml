@model IEnumerable<school_management.Models.TeachersEnrolleds>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.enrolledstatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idTeacher)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idCourse)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.enrolledstatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idTeacher)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idCourse)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
